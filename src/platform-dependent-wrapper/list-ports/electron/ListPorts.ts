export { getSerialPorts } from '../../../device/serial/serial-port';
export { listBesstDevices } from '../../../device/besst/besst';
export async function requestPort(baudRate: number): Promise<boolean> {
    return false;
}
